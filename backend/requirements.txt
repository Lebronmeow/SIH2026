# ORCA backend dependencies (Python >= 3.11, developed on 3.13)
# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5

# HTTP
httpx>=0.27

# Ocean science stack
erddapy>=2.2
xarray>=2025.1
netcdf4>=1.7.2
numpy>=2.0
scipy>=1.14
pandas>=2.2

# Geospatial
geopandas>=1.1
shapely>=2.0.4
pyproj>=3.7
geojson-pydantic>=1.1

# AIS decoding (optional input path)
pyais>=3.2

# Persistence (SQLite by default; PostGIS optional via docker-compose)
sqlalchemy>=2.0
geoalchemy2>=0.15
# psycopg[binary]>=3.2   # uncomment for PostgreSQL/PostGIS (installed in Docker image)

# Dev / test
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.6
